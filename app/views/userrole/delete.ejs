<h2 class="text-danger">Delete</h2>
<fieldset>
    <legend>userRole</legend>
    
      

    <% form_for('userRole', {controller : '/userRole', action: '/delete'}, function(f){ %>
        <%- f.hidden_field("id") %>
        <p>
            <%- f.submit("Delete", {class : 'btn btn-danger'}) %>
        </p>
    <% }) %>
</fieldset>

<%- link_to('back to list', '/userRole', {class : 'btn btn-primary'}) %>