<h2 class="text-danger">Delete</h2>
<fieldset>
    <legend>recolement</legend>
    
      

    <% form_for('recolement', {controller : '/recolement', action: '/delete'}, function(f){ %>
        <%- f.hidden_field("id") %>
        <p>
            <%- f.submit("Delete", {class : 'btn btn-danger'}) %>
        </p>
    <% }) %>
</fieldset>

<%- link_to('back to list', '/recolement', {class : 'btn btn-primary'}) %>