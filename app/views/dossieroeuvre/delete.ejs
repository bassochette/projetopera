<h2 class="text-danger">Delete</h2>
<fieldset>
    <legend>dossierOeuvre</legend>
    
      

    <% form_for('dossierOeuvre', {controller : '/dossierOeuvre', action: '/delete'}, function(f){ %>
        <%- f.hidden_field("id") %>
        <p>
            <%- f.submit("Delete", {class : 'btn btn-danger'}) %>
        </p>
    <% }) %>
</fieldset>

<%- link_to('back to list', '/dossierOeuvre', {class : 'btn btn-primary'}) %>